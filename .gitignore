node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Paraglide (compiled i18n runtime — generated from messages/ + project.inlang)
/src/lib/paraglide

# Cloudflare
.dev.vars

# Throwaway local D1 built by the E2E harness (tests/e2e/seed.ts)
.wrangler-e2e

# Playwright
/test-results
/playwright-report
/blob-report
/playwright/.cache
# Per-deployment Cloudflare config (resource IDs). Copy wrangler.toml.example to
# wrangler.toml and fill in your own.
/wrangler.toml

# Pencil generated images
/images/

# Exported mockups (shared externally, not source)
/docs/fursuit-photos-mockups/

# Claude Design bundle (HTML exported from the design file + synced to claude.ai/design)
/design/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude Code local config
.claude/

# Ship review artifacts (screenshots, gate output — not source)
.ship/

# Raw sticker downloads from dev/import (re-downloadable; not source)
sticker-assets/

# Review-loop screenshots + artifacts (generated by the ship review, not source)
.ship/

# Pencil design files — kept local only (Pencil deprecated; source of truth is Claude Design)
*.pen
.ship/
